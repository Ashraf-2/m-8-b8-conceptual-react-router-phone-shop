import { Link } from "react-router-dom";

const Logo = () => {
    return (
        <div>
            <Link to="/" className="text-xl font-semibold">Phone Shop</Link>
        </div>
    );
};

export default Logo;